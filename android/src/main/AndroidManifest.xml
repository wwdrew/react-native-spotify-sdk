<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <meta-data android:name="spotifyClientId" android:value="${spotifyClientId}" />
    <meta-data
      android:name="spotifyRedirectUri"
      android:value="${spotifyRedirectUri}" />
  </application>
</manifest>
